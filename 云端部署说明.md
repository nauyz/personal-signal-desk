# 免费云端版本

## 首屏加载

发布时通过 `prerender.cjs` 复用前端模板，将最新 12 条内容写入 HTML。未加载脚本或数据请求失败时，仍可阅读并打开原文；筛选和搜索准备好后才启用。需要 Node.js（工作流已配置 Node 24）。

各栏目数据以带内容指纹的 JSON 分开发布。首页先加载内容数据，首次页面就绪后，在空闲时以 2 路低优先级请求预加载其余栏目（优先默认榜单，再加载其他范围）。点击与预加载共享缓存和进行中的请求；预加载失败不影响当前页面，点击时会重试。后台更新目录后会补充新版本数据。`data/site.json` 保留兼容旧客户端，新版不请求整站包。资源地址随内容变化，避免浏览器复用旧代码。

## 分源调度（当前规则）

每小时第 17 分钟检查到期任务，未到期跳过。间隔按上次尝试开始时间计算，失败也等待相同间隔。调度延迟或单轮采集耗时会使实际时间略晚；Actions 缓存被淘汰后会重新初始化各任务。

| 任务 | 最短间隔 |
|---|---|
| AIHOT 内容、事实；HN；X 推文、热点；PH 今日 | 1 小时 |
| GitHub Trending | 3 小时 |
| AIHOT 日报；X 账号；PH 昨日、近 7 日、近 30 日 | 6 小时 |
| PH 14 个垂类（包括失败重试） | 12 小时 |

翻译随所属数据任务执行，仅请求没有有效缓存或原文变化的条目。原有数据和译文在任务未到期时保留。具体频率统一维护于 `cloud_build.py` 的 `CADENCE`，仅影响云端采集，本地 `server.py --sync` 沿用本地调度逻辑。

已发布： https://nauyz.github.io/personal-signal-desk/

公开仓库： https://github.com/nauyz/personal-signal-desk

本地 `.cloud-release` 是经过筛选的发布目录，具有独立历史；主项目的旧历史没有上传。后续发布应同步运行文件到这个目录，勿直接推送主项目历史。

两轮实测：Product Hunt API、GitHub Trending、HN 及 X 榜单可在云端更新，X 推文七日榜第二轮已成功获取 87 条。Product Hunt 分类页抓取失败时使用 `public_snapshots.json` 中原有公开快照，页面明确提示并保留获取时间；这部分不能保证每小时更新。

页面由 GitHub Pages 提供；GitHub Actions 每小时第 17 分钟检查上述到期任务。你的电脑不需要开机。定时任务可能延迟，不承诺准点。

## 第一次部署

1. 在 GitHub 创建公开仓库，并将本项目推送进去。公开前检查历史文件；不要上传 Token、数据库、日志或个人配置。
2. 仓库 Settings → Secrets and variables → Actions → New repository secret，创建 `PRODUCTHUNT_TOKEN`，填入 Developer Token。不要写入源码。
3. Settings → Pages → Build and deployment → Source 选择 GitHub Actions。
4. Actions → Collect and publish signal desk → Run workflow。
5. 成功后打开 deploy 任务给出的网址。推荐分享 `网址/#/launches`，其他页面同样使用 `#/facts`、`#/content` 等地址。

缺少 Product Hunt Token 时其他来源仍可采集，Product Hunt API 榜单不会更新。需在云端首次运行确认各来源和翻译接口可达；某些来源可能限制云端 IP。

## 数据与失败处理

- SQLite 及翻译结果通过 Actions cache 在采集轮次之间复用。缓存可能被平台淘汰；它不是永久备份。淘汰后下轮重建数据和翻译。
- 只向 Pages 上传 `dist` 中的前端文件和公开数据，不发布数据库、Token 或原始错误信息。
- 某个来源采集失败时尽可能保留缓存中的旧快照。整个任务失败或超时则不会发布，线上继续展示上次成功版本。
- 运行最多 50 分钟，工作流串行执行。单个采集线程池最多 2 个工作线程。
- 数据更新时间与发布时间不同，页面的来源时间来自采集快照。
- 公开仓库长时间没有活动可能自动暂停定时任务，请在面试前确认最近运行状态。
- 暂不引入付费翻译或额外云数据库。免费额度和数据源接口政策以平台实际规则为准。

## 本地验证静态版本（不采集）

```powershell
cd 'D:\ai_project2\个人信源网站'
python cloud_build.py
python -m http.server 8878 --directory dist --bind 127.0.0.1
```

打开 `http://127.0.0.1:8878/#/launches`。使用已有数据库导出，缺数据的页面显示空列表。

原来的 `python server.py` 本地缓存模式仍可使用。`cloud_build.py --collect` 会执行联网采集，通常仅在云端运行。

## 现阶段限制

这是静态阅读站：搜索和交叉筛选在浏览器完成，保留双语与官方外链，不支持登录、个人云端收藏或用户写入。数据整包发布，随着数据增长需监测大小并按页面拆包。Actions 缓存是加速机制，不能当作可靠归档库。
